// Embedded Avon rate card — reuses the real app page. The embed context (from
// the layout) makes its hooks take the key-authenticated Silverco path.
export { default } from "@/app/(app)/[entityId]/inquiries/rate-card/page";
