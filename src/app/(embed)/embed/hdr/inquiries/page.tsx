// Embedded HDR pipeline board — reuses the real app page. The embed context
// (from the layout) makes its hooks/nav take the key-authenticated HDR path.
export { default } from "@/app/(app)/[entityId]/inquiries/page";
